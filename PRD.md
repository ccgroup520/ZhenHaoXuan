# 产品需求文档 (PRD) — 真好选 (ZhenHaoXuan)

# 第一部分：PM 视角

## 1. 产品定位

**真好选** 是一款 iOS 应用，帮助用户从视频和 Live Photo 中快速提取静态画面，保存到相册。

**一句话描述：** 简单地从视频中抓取高清画面。

**核心价值：**
- 操作简单，不需要专业工具
- 画质无损，跟原视频一样清晰
- 所有处理在手机上完成，不上传任何数据

---

## 2. 功能总览（按用户操作流程排列）

### 2.1 首页

用户打开 App 首先看到首页，包含：
- **品牌标识** — 应用图标和名称
- **选择视频按钮** — 最主要的操作入口，点击后打开相册
- **导出统计** — 显示今日剩余导出次数（如"剩余 3 张"或"无限导出"）
- **会员身份** — 如果是会员，显示会员徽章

视觉效果：干净清爽的卡片式布局，背景有品牌色渐变。

### 2.2 选择视频 / Live Photo

从系统相册选择要处理的素材。

**支持的素材类型：**
- 视频（mov、mp4 等常见格式）
- Live Photo（实况照片）

**交互流程：**
1. 点击"选择视频或 Live Photo"
2. 弹出系统相册选择器
3. 选中后自动进入预览界面
4. 如果视频在 iCloud 上，自动下载并提示"正在加载"

**长视频优化：**
- 长视频加载时，进度条上方显示"正在优化视频..."，让用户知道App在处理
- 如果视频超过5分钟，首次播放时在顶部温和提示："视频较长，拖动进度条可快速定位"
- 不强制限制时长，不弹窗打断用户操作

### 2.3 视频预览与帧捕获

这是核心操作界面，全屏播放视频，底部提供操作按钮。

**播放控制：**
- 单击画面切换 播放/暂停
- 进度条拖动（拖拽时画面实时跟随）
- 前进/后退一帧（逐帧精确步进）
- 静音开关

**帧捕获：**
- 点击中间大按钮捕获当前画面
- 捕获成功有震动反馈（瞬间感知成功）
- 捕获按钮上方显示已捕获数量（红点角标，如"3"表示已捕获3张）
- 不额外显示"已截取"等文字提示，保持界面简洁

**画面效果：**
- 视频背景自动提取主色调，生成动态氛围光效
- 如果视频是 HDR 内容，右上角显示 HDR 标识

### 2.4 帧管理与导出

捕获的帧会展示在画廊界面，进行管理和导出。

**功能列表：**
- 网格展示所有已捕获的帧（缩略图 + 时间戳 + 视频来源）
- 按捕获时间倒序排列（最新捕获的在最前面）
- 点击选择/取消选择
- 多选后批量导出或批量删除
- 单帧可通过长按菜单操作（导出/选择/删除）

**导出相关：**
- 导出时显示"正在导出…"提示
- 导出成功提示"图片已保存到相册"
- 导出照片命名格式：`视频名_时间点`（如：生日party_01m23s.jpg）
- 达到每日限额时提示升级会员

### 2.5 设置

**个人资料：**
- Apple 登录（登录后显示头像和昵称）
- 会员信息展示（会员图标、等级）

**导出设置：**
- 格式选择：PNG / JPEG（默认JPEG）
- 质量选择：无损 / 高 / 中 / 低（默认高）
- HDR 导出开关（仅付费会员可用，免费用户显示锁定并引导去会员页）
- 设置后永久生效，不需要每次导出时都选
- 选择无损PNG导出时，提示用户"无损格式导出速度较慢，请耐心等待"
- 免费用户点击HDR开关，弹提示框"HDR导出需要开通会员"，带"查看会员方案"按钮

**会员入口：** 非会员显示"升级会员"推广卡片

### 2.6 会员购买

会员购买页面包含：
- **会员特权列表** — 清晰列出会员能享受的所有好处
- **三档方案选择卡** — 月度/年度/永久，选中后高亮
- **购买按钮** — 显示对应方案的价格
- **恢复购买** — 换设备或重装后恢复会员资格

---

## 3. 界面设计规范

### 3.1 视频播放界面布局
- **左上角**：返回按钮
- **右上角**：HDR标识（仅HDR视频显示）、静音开关
- **进度条上方**：当前时间/总时长
- **底部**：播放控制栏（居中对齐）
- **捕获按钮**：底部居中，放大显示，带数量角标

### 3.2 视觉风格
- 所有图标使用 SF Symbols（苹果原生图标）
- 按钮使用半透明毛玻璃效果，不遮挡视频内容
- 氛围光效：提取视频主色，背景柔和渐变，随播放缓慢变化
- 背景色纯白或浅灰，界面干净清爽

---

## 4. Live Photo 处理

### 4.1 导入流程
- 在相册选择器中，Live Photo 右上角显示"Live"小标识
- 用户点选后，自动提取视频部分进入播放界面
- 播放界面顶部显示"来自 Live Photo"提示

### 4.2 技术实现
- Live Photo 视频部分通常只有 3 秒，无需特殊处理
- 按正常视频流程处理即可

---

## 5. 错误处理与用户引导

### 5.1 相册权限处理
- 用户拒绝授权时，显示引导页："需要相册权限才能选择视频"
- 提供"去设置"按钮，引导用户开启权限
- 不在首页就弹窗要权限，用户点"选择视频"时再申请

### 5.2 视频加载失败
- iCloud 视频下载失败时，提示"视频加载失败，请检查网络后重试"
- 提供重试按钮，不要直接返回首页

### 5.3 新手引导
- 首次使用时，在视频播放界面显示一次性引导气泡："点击画面播放/暂停"
- 用户第一次捕获帧时，显示"已捕获，可继续抓取更多"
- 只在首次操作时显示，之后不再出现

---

## 6. 品牌设计

### 6.1 品牌色
- **主色**：深蓝或渐变紫色（科技感、专业感）
- **辅助色**：橙黄色（用于强调按钮、会员标识）
- **背景色**：纯白或浅灰（干净清爽）

### 6.2 App 图标
- 设计元素：播放按钮 + 照片轮廓组合
- 风格：简洁现代，一眼识别是视频转图片工具

### 6.3 整体风格原则
- 界面简洁，无花哨装饰
- 所有操作直观，新用户 3 秒内找到"选择视频"按钮
- 会员购买页要有高级感，让用户觉得值得付费

---

## 3. 会员方案

### 3.1 会员等级一览

| 等级 | 价格 | 每日导出上限 | HDR 导出 |
|------|------|:-----------:|:--------:|
| 👤 普通用户 | 免费（永久） | 3 张 | ❌ |
| 🥇 月度会员 | ¥6/月 | 30 张 | ✅ |
| 🥈 年度会员 | ¥28/年 | 100 张 | ✅ |
| 👑 永久会员 | ¥68（一次性） | 无限 | ✅ |

### 3.2 所有用户都享有的权益
- 无损原图导出
- 4K / 原始分辨率
- 无水印
- 批量导出
- EXIF 信息保留

### 3.3 会员定价逻辑

| 方案 | 日均成本 | 单张导出成本 |
|------|---------|------------|
| 免费 | ¥0 | — |
| 月度 ¥6/月 | ¥0.20/天 | ¥0.007/张 |
| 年度 ¥28/年 | ¥0.08/天 | ¥0.0008/张 |
| 永久 ¥68 | 一次性 | 无限 |

**设计思路：**
- 免费 3 张/日 → 足够体验完整功能
- 月度 30 张/日 → 轻度用户的日常使用
- 年度 100 张/日 → 重度用户，日均成本仅 8 分钱（强烈推荐）
- 永久 ¥68 → 一次付费终身使用，约 2.5 年回本

### 3.4 购买说明
- 月度/年度 = 自动续期订阅，可随时取消
- 永久 = 一次性买断
- 支持恢复购买
- 付款通过 Apple ID 处理

### 3.5 会员购买页设计
- 新用户首次打开App时，会员页显示"限时优惠：永久会员立减10元，仅需¥58"
- 优惠显示7天倒计时，营造紧迫感
- 年度会员标注"推荐"标签，提示"最划算"
- 达到每日导出限额时，当天不可再导出，弹提示"今日导出次数已用完"，带"查看会员方案"按钮

---

# 第二部分：开发视角

## 4. 技术架构

### 架构模式
MVVM（Model-View-ViewModel）+ Actor 并发安全

### 架构分层

```
┌──────────────────────────────────────────────────────┐
│              UI 层 (SwiftUI)                          │
│  ContentView (TabView: 首页 + 设置)                   │
│  ├── HomeView                   首页                  │
│  ├── VideoPickerView           相册选择器封装          │
│  ├── VideoPreviewView          全屏播放+帧捕获         │
│  │   └── DraggableProgressBar  可拖拽进度条            │
│  ├── FrameGalleryView          帧画廊+导出             │
│  ├── VIPMembershipView         会员购买                │
│  └── SettingsView              设置+登录+导出配置       │
├──────────────────────────────────────────────────────┤
│            ViewModel 层                               │
│  VideoPlayerViewModel         播放控制+帧导航          │
│  FrameCaptureViewModel        帧捕获逻辑               │
│  FrameGalleryViewModel        帧导出逻辑               │
├──────────────────────────────────────────────────────┤
│            Manager 层                                 │
│  VIPManager                   IAP购买+会员状态+配额    │
│  UserProfileManager           Apple登录+用户资料       │
├──────────────────────────────────────────────────────┤
│            Service 层 (Actor — 线程安全)               │
│  FrameExtractorService        帧提取                   │
│  FrameStore                   帧持久化+缩略图管理       │
│  ImageExportService           图片导出到相册            │
│  LivePhotoExtractorService    Live Photo提取           │
│  MediaTempStore               导入视频临时文件管理      │
│  KeychainManager              加密存储                 │
├──────────────────────────────────────────────────────┤
│            系统框架                                    │
│  AVFoundation                  视频处理                │
│  PhotosUI / PhotoKit           相册访问                │
│  StoreKit 2                    内购                    │
│  AuthenticationServices        Apple登录               │
│  UIKit / ImageIO               图片处理                │
│  Keychain Services             加密存储                │
└──────────────────────────────────────────────────────┘
```

### 技术栈

| 技术 | 用途 |
|------|------|
| Swift 5.9+ | 开发语言 |
| SwiftUI (iOS 16+) | UI 框架 |
| AVFoundation | 视频播放、帧提取 |
| UIKit / ImageIO | 图片处理、导出编码 |
| PhotosUI / PhotoKit | 相册选择与写入 |
| StoreKit 2 | 内购与会员验证 |
| AuthenticationServices | Apple 登录 |
| Keychain Services | 敏感数据加密存储 |
| CIFilter (CIAreaAverage) | 视频主色提取 |
| MVVM + Actor | 架构模式与并发安全 |

## 5. 数据模型

### CapturedFrame（捕获的帧）
```swift
struct CapturedFrame {
    id: UUID              // 唯一标识
    timestamp: Double     // 在视频中的时间位置（秒）
    videoName: String     // 来源视频名称
    captureDate: Date     // 捕获时间
    imageFileName: String // 原图在磁盘上的文件名（导出时按需读取）
    thumbnail: UIImage    // 缩略图（用于列表展示，内存友好）
    source: FrameSource   // 来源类型：普通视频 或 Live Photo
}
```

### FrameSource（帧来源）
- `.video(url: URL)` — 从普通视频捕获
- `.livePhoto(assetID: String)` — 从 Live Photo 提取

### ExportSettings（导出设置）
```
- format: PNG / JPEG       （用户可调）
- quality: 无损 / 高 / 中 / 低 （用户可调）
- hdrEnabled: true / false  （仅会员可用）
```

### VIPTier（会员等级）
```
- free      普通用户（免费）
- monthly   月度会员（¥6/月）
- yearly    年度会员（¥28/年）
- permanent 永久会员（¥68一次性）
```

## 6. 安全与隐私
- 所有视频处理在本地完成，不上传任何数据
- 会员信息 Keychain 加密存储（防篡改）
- 导出配额使用 UserDefaults（非敏感数据）
- 启动时通过 StoreKit 2 验证会员资格
- 仅申请相册读取/写入权限


> **文档版本：v4.0**
> **最后更新：** 2026-07-16

# 产品需求文档 (PRD) - 真好选 (ZhenHaoXuan)

## 1. 执行摘要

### 问题陈述
普通用户在观看视频时经常想要提取特定画面作为静态图片，但现有工具要么操作复杂、要么提取速度慢、要么画质损失严重，缺乏一个简单直观的解决方案。

### 解决方案
开发一款iOS原生应用，让用户能够从短视频（≤30秒）中快速、高质量地捕获任意帧，提供类似苹果相册的流畅交互体验，实现一键导入、预览、捕获和导出的完整流程。

### 成功标准
- MVP版本在2周内完成核心功能开发
- 帧提取速度 < 3秒（对于30秒视频）
- 帧导出成功率达到 100%
- 用户能在无指导情况下完成首次帧捕获和导出
- 应用崩溃率 < 0.1%

---

## 2. 用户体验与功能

### 用户画像
**主要用户：普通大众用户**
- 年龄：18-45岁
- 特征：熟悉智能手机基本操作，习惯使用相册应用
- 需求：快速从短视频中提取精彩瞬间，操作简单直观
- 痛点：现有截图工具画质差、时间点不准确

### 用户故事

#### 故事1：视频导入
**作为** 用户  
**我想要** 从相册中选择一个短视频  
**以便于** 开始提取视频中的精彩画面  

**验收标准：**
- 支持从系统相册选择视频
- 仅显示≤30秒的视频（或提示裁剪）
- 支持常见视频格式（MP4, MOV, M4V）
- 支持iCloud视频下载
- 选择后自动进入预览界面

#### 故事2：视频预览
**作为** 用户  
**我想要** 流畅地预览和定位视频中的特定画面  
**以便于** 精确找到想要捕获的帧  

**验收标准：**
- 黑色背景，视频全屏显示
- 时间显示格式：MM:SS.CC（分:秒.百分之一秒）
- 进度条为白色细长条（4pt高度）
- 拖动进度条时放大至8pt，松开恢复
- 拖动时画面实时同步（60fps流畅度）
- 支持播放/暂停控制
- 支持手势控制（单击播放/暂停）

#### 故事3：帧捕获
**作为** 用户  
**我想要** 快速捕获当前预览的画面  
**以便于** 保存这个精彩瞬间  

**验收标准：**
- 提供明显的捕获按钮
- 提取时间 < 3秒
- 图片质量与原视频一致（可设置）
- 捕获成功时有触觉反馈
- 显示已捕获帧的数量
- 捕获后可继续预览视频

#### 故事4：帧预览与导出
**作为** 用户  
**我想要** 查看已捕获的帧并导出到相册  
**以便于** 在其他应用中使用这些图片  

**验收标准：**
- 提供帧预览界面，显示所有已捕获的帧
- 支持删除不需要的帧
- 支持选择导出格式（PNG/JPG）
- 一键导出到系统相册
- 导出成功后显示提示
- 支持批量导出

### 非目标（MVP阶段不包含）
- 视频编辑功能（裁剪、滤镜等）
- 视频录制功能
- 云端同步
- 社交分享功能
- 付费高级功能
- iPad适配（仅iPhone）

---

## 3. 技术规格

### 架构概览

```
┌─────────────────────────────────────────┐
│           用户界面层 (SwiftUI)            │
├─────────────────────────────────────────┤
│  ContentView                            │
│  ├── VideoPickerView (视频选择)         │
│  ├── VideoPreviewView (视频预览)        │
│  ├── FrameCaptureView (帧捕获)          │
│  └── FrameGalleryView (帧预览导出)      │
└─────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────┐
│           业务逻辑层 (ViewModel)         │
├─────────────────────────────────────────┤
│  VideoPlayerViewModel                   │
│  FrameCaptureViewModel                  │
│  FrameGalleryViewModel                  │
└─────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────┐
│           服务层 (Services)              │
├─────────────────────────────────────────┤
│  VideoImportService (视频导入)          │
│  FrameExtractorService (帧提取)         │
│  ImageExportService (图片导出)          │
└─────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────┐
│           系统框架层                      │
├─────────────────────────────────────────┤
│  AVFoundation (视频处理)                │
│  PhotosUI (相册访问)                    │
│  UIKit (图片处理)                       │
└─────────────────────────────────────────┘
```

### 技术栈
- **开发语言**: Swift 5.9+
- **UI框架**: SwiftUI (iOS 16+)
- **视频处理**: AVFoundation
- **图片处理**: UIKit, ImageIO
- **相册访问**: PhotosUI, PhotoKit
- **架构模式**: MVVM (Model-View-ViewModel)
- **依赖管理**: Swift Package Manager (SPM)

### 关键技术实现

#### 1. 视频导入
```swift
// 使用PHPickerViewController选择视频
// 配置选择器仅显示视频，限制时长≤30秒
// 处理iCloud视频下载
```

#### 2. 帧提取
```swift
// 使用AVAssetImageGenerator
// 配置精确时间点提取
// 支持高质量图片输出
// 异步处理，避免阻塞UI
```

#### 3. 进度条交互
```swift
// 使用自定义ProgressView
// Gesture处理拖动
// CADisplayLink实现60fps刷新
// 状态管理控制进度条宽度
```

#### 4. 图片导出
```swift
// 使用PHPhotoLibrary保存到相册
// 支持PNG/JPG格式转换
// 请求相册访问权限
```

### 数据模型

#### VideoInfo
```swift
struct VideoInfo {
    let url: URL
    let duration: TimeInterval
    let creationDate: Date?
    let thumbnail: UIImage?
}
```

#### CapturedFrame
```swift
struct CapturedFrame: Identifiable {
    let id: UUID
    let image: UIImage
    let timestamp: TimeInterval
    let videoName: String
    let captureDate: Date
}
```

### 集成点
- **Photos Framework**: 读取相册视频，保存图片
- **AVFoundation**: 视频播放、帧提取
- **UIKit**: 图片处理、触觉反馈

### 安全与隐私
- 请求相册访问权限（读写）
- 不收集用户数据
- 所有处理在本地完成
- 不上传任何内容到服务器

---

## 4. MVP范围定义

### MVP核心功能
1. ✅ 视频导入（从相册选择≤30秒视频）
2. ✅ 视频预览（基础播放、暂停、进度显示）
3. ✅ 基础帧捕获（点击按钮捕获当前帧）
4. ✅ 帧预览和导出（查看已捕获帧、导出到相册）

### MVP暂不包含
- ❌ 进度条拖动交互（简化为播放/暂停）
- ❌ 触觉反馈
- ❌ 图片质量设置
- ❌ 时间格式精确到百分之一秒
- ❌ iCloud视频支持（仅本地视频）

### MVP验收标准
- 用户能选择本地视频（≤30秒）
- 用户能播放、暂停视频
- 用户能捕获当前帧
- 用户能查看已捕获的帧
- 用户能导出帧到相册
- 整个流程无崩溃

---

## 5. 风险与路线图

### 技术风险
1. **帧提取性能**: 大视频可能提取慢
   - 缓解：限制视频时长≤30秒
   
2. **内存占用**: 多帧图片可能占用大量内存
   - 缓解：限制最大捕获数量，使用缩略图预览

3. **相册权限**: 用户可能拒绝授权
   - 缓解：友好的权限引导界面

### 开发路线图

#### 第1阶段：MVP开发（2周）
- Week 1: 视频导入、预览、基础帧捕获
- Week 2: 帧预览、导出、测试优化

#### 第2阶段：v1.1（1周）
- 进度条拖动交互
- 触觉反馈
- 图片质量设置
- iCloud视频支持

#### 第3阶段：v1.2（1周）
- 时间格式精确显示
- 性能优化
- UI/UX优化
- 错误处理完善

#### 第4阶段：v2.0（未来）
- iPad适配
- 视频编辑功能
- 云端同步
- 社交分享

---

## 6. 测试策略

### 单元测试
- FrameExtractorService帧提取逻辑
- 时间格式转换
- 图片格式转换

### 集成测试
- 视频选择到帧捕获完整流程
- 帧捕获到导出完整流程
- 权限处理流程

### UI测试
- 用户交互流程测试
- 边界条件测试（空状态、错误状态）

### 性能测试
- 帧提取速度测试（目标<3秒）
- 内存占用测试
- 60fps流畅度测试

---

## 7. 成功指标

### 定量指标
- 帧提取成功率 ≥ 99%
- 帧提取平均时间 < 3秒
- 应用崩溃率 < 0.1%
- 用户首次使用成功率 ≥ 90%

### 定性指标
- 用户能在无指导下完成首次操作
- 交互流畅，无明显卡顿
- 导出图片质量满足用户需求

---

## 8. 附录

### 参考应用
- 苹果相册应用（交互设计参考）
- Frame Grabber（功能参考）
- Video to Photo（功能参考）

### 设计资源
- Apple Human Interface Guidelines
- SF Symbols图标库

### 文档版本
- v1.0 - 2026-04-07 - 初始版本

# 真好选 (ZhenHaoXuan) 架构审查报告

**审查日期**: 2026-07-12
**审查人**: 架构师角色
**项目阶段**: MVP → v1.2 过渡期

---

## 📋 目录

- [第一部分：项目现状总览](#第一部分项目现状总览)
- [第二部分：任务 A — 数据策略审查](#第二部分任务-a--数据策略审查)
- [第三部分：任务 B — Live Photo 提取功能设计](#第三部分任务-b--live-photo-提取功能设计)
- [第四部分：综合优先级排期建议](#第四部分综合优先级排期建议)

---

## 第一部分：项目现状总览

### 架构全景图（通俗版）

```
┌──────────────────────────────────────────────┐
│  你看到的屏幕（界面层）                         │
│  首页 · 视频选择 · 预览播放 · 帧画廊 · 设置      │
├──────────────────────────────────────────────┤
│  大脑（ViewModel 层）                          │
│  管理播放状态 · 管理捕获队列 · 管理导出流程       │
├──────────────────────────────────────────────┤
│  工具箱（Service 层）                          │
│  视频帧提取 · 图片存盘 · 导出到相册 · 会员管理    │
├──────────────────────────────────────────────┤
│  仓库（数据层）                                │
│  磁盘文件存储 · UserDefaults 配置 · Keychain    │
└──────────────────────────────────────────────┘
```

**打个比方**：这个 App 就像一个**自助拍照亭**——顾客选一段视频（选照片），在屏幕上找到想要的画面（预览），按下快门（捕获帧），最后打印出来（导出到相册）。整个过程中照片从不出亭子（纯本地处理）。

### 当前技术栈

| 层面 | 用到的工具 | 相当于 |
|------|-----------|--------|
| UI | SwiftUI (iOS 16+) | 搭界面的积木 |
| 视频处理 | AVFoundation | 播放和拆解视频的引擎 |
| 图片处理 | UIKit + ImageIO | 处理照片的暗房工具 |
| 相册访问 | PhotosUI / PhotoKit | 和系统相册对话的窗口 |
| 支付 | StoreKit 2 | 和苹果收银台的接口 |
| 数据存储 | UserDefaults + 磁盘文件 | 小记事本 + 文件柜 |

---

## 第二部分：任务 A — 数据策略审查

### 🔴 问题 1：会员数据存储像"把保险柜密码写在便利贴上"

**风险等级**: 🔴 高 | **优先级**: P0（立即处理）

#### 通俗解释

当前你把"这个用户是不是永久会员"这个信息存在 `UserDefaults` 里（相当于 iOS 系统给每个 App 配的免费小记事本）。问题是：

- **这个记事本不加密，也不上锁**。如果有人把 iPhone 连电脑、用免费工具打开 App 的数据文件，找到 `vipTier = "free"` 改成 `vipTier = "permanent"`，重启 App 就变成永久会员了。
- **更糟糕的是**，购买验证也全在本地做。正规做法是把苹果返回的"购买收据"发给你的服务器二次确认，就像超市收银员刷完卡后要等银行回执一样。但你的 App 没有服务器，所以坏人可以伪造这个"已付款"的信号。

#### 代码病灶

`VIPManager.swift:186-203` — `loadVIPStatus()` 和 `saveVIPStatus()` 直接读写 `UserDefaults`，没有任何校验：

```swift
// 当前代码（不安全）—— 相当于把"会员到期日"写在可涂改的纸条上
private func loadVIPStatus() {
    if let tierString = UserDefaults.standard.string(forKey: vipTierKey),
       let tier = VIPTier(rawValue: tierString) {
        currentTier = tier   // ⚠️ 直接信任这个值，不验证真伪
    }
}
```

#### 重构方案

**分两步走**（不需要服务器也能大幅提升安全性）：

**第一步**：把敏感字段从 `UserDefaults` 搬到 `Keychain`。Keychain 是苹果提供的加密保险柜，数据存在加密芯片里，越狱也很难破解。

```
UserDefaults（便利贴） → 只放 UI 偏好（导出格式、主题颜色）
Keychain（保险柜）     → 放 VIP 等级、到期时间、购买收据
```

**第二步**：增加本地收据验证。苹果的 StoreKit 每次购买后会给一张"数字收据"。我们在 App 启动时，让苹果官方验证一下这张收据是不是真的，防止伪造。

伪代码：

```swift
// 改造后的 VIPManager 初始化流程
private init() {
    // 1. 从 Keychain 读取（而非 UserDefaults）
    let savedTier = KeychainManager.shared.getVIPStatus()  // 加密保险柜
    
    // 2. 启动时向苹果验证收据真伪
    Task {
        let isValid = await verifyReceiptWithApple()  // 找苹果确认
        if isValid {
            self.currentTier = savedTier
        } else {
            self.currentTier = .free  // 验证失败，降级
            KeychainManager.shared.clearVIPStatus()
        }
    }
}
```

> 💡 **对你的影响**：用户无法通过修改文件免费获得会员，减少收入损失。改动大约需要 2-3 天开发。

---

### 🔴 问题 2：混用三种线程管理模式，容易造成"三个厨师炒一盘菜"

**风险等级**: 🔴 高 | **优先级**: P1（本月内处理）

#### 通俗解释

你的代码里同时用了三种"指挥干活"的方式：

| 方式 | 相当于 | 代码里出现的地方 |
|------|--------|-----------------|
| **GCD** (`DispatchQueue`) | 喊一嗓子"小王你去干这个" | 帧捕获、导出、文件删除 |
| **Combine** (`@Published`) | 贴个便利贴"这个值变了通知我" | ViewModel 属性绑定 |
| **async/await** | 写个流程清单，按顺序执行 | 视频加载、HDR 提取 |

三种方式混在一起就像：**一个厨房里有中餐厨师、西餐厨师、日料厨师同时在炒同一盘菜**——指令互相冲突，最终可能数据错乱甚至崩掉（虽然现在还没崩，但这是"定时炸弹"）。

#### 代码病灶（具体到行）

**病灶 1**: `FrameCaptureViewModel.swift:28` — GCD 和 Combine 的"接力棒交接"有漏洞：

```swift
// 当前代码
frameExtractor.extractFrame(from: videoURL, at: time) { [weak self] result in
    // 回调可能在任意线程
    DispatchQueue.global(qos: .userInitiated).async {
        let frame = self.frameStore.add(image: image, ...)  // FrameStore 内部有 NSLock
        DispatchQueue.main.async {
            self.isCapturing = false
            self.capturedFrames.append(frame)  // @Published 触发 UI 刷新
        }
    }
}
```

问题：如果用户快速连按 5 次捕获按钮，5 个 `DispatchQueue.global` 任务几乎同时执行，都去调用 `FrameStore.add()`。虽然 `FrameStore` 有 `NSLock` 保护 `index.json`，但 `UIImage.pngData()` 压缩是 CPU 密集型操作且在锁外面——可能造成图片写入顺序错乱。

**病灶 2**: `FrameExtractorService.swift:26-60` — `AVAssetImageGenerator` 的回调是系统线程，但在里面混用了 `Task` (async/await) 和 `DispatchQueue.main.async`：

```swift
// 当前代码 — 回调线程 → GCD 主线程 → Task → MainActor，绕了三道弯
imageGenerator.generateCGImagesAsynchronously(...) { _, cgImage, _, _, _ in
    // 这里是系统回调线程（不知道是哪条）
    DispatchQueue.main.async {
        completion(.success(image))     // 第一道弯
    }
    // ... 或者 ...
    Task {                               // 第二道弯
        if let hdrImage = await self?.extractHDRFrame(...) {
            await MainActor.run {        // 第三道弯
                completion(.success(hdrImage))
            }
        }
    }
}
```

#### 重构方案

**目标**：统一用 `async/await` + `@MainActor`（苹果最新的推荐模式），逐步淘汰 GCD 的 `DispatchQueue`。

核心思路 — 让 Swift 的 `actor` 充当**专属收银台**，一次只服务一个人：

```swift
// 改造后的 FrameCaptureViewModel
// actor = 一把自动锁，同一时间只有一个任务能进入
actor FrameStoreActor {
    private let store = FrameStore.shared
    
    func add(image: UIImage, timestamp: TimeInterval, videoName: String) -> CapturedFrame? {
        // 不需要手写 NSLock！actor 保证一次只进来一个调用
        return store.add(image: image, timestamp: timestamp, videoName: videoName)
    }
}

@MainActor  // 所有 UI 更新自动在主线程
class FrameCaptureViewModel: ObservableObject {
    private let storeActor = FrameStoreActor()
    
    func captureFrame(from videoURL: URL, at time: TimeInterval, videoName: String) {
        isCapturing = true
        
        Task {
            do {
                // async/await 模式：代码看起来像同步，实际异步执行
                let image = try await frameExtractor.extractFrame(from: videoURL, at: time)
                let frame = await storeActor.add(image: image, timestamp: time, videoName: videoName)
                
                // 自动回到主线程（@MainActor 保证）
                self.isCapturing = false
                if let frame = frame {
                    self.capturedFrames.append(frame)
                }
            } catch {
                self.isCapturing = false
            }
        }
    }
}
```

> 💡 **对你的影响**：代码更安全、不容易崩。开发体验更好（少写很多回调嵌套）。改动覆盖 8 个文件，需要约 5-7 天开发 + 测试。

---

### 🟡 问题 3：存储空间会"只进不出"，长期使用可能占满手机

**风险等级**: 🟡 中 | **优先级**: P1（本月内处理）

#### 通俗解释

你的 App 有两个"仓库"：

| 仓库 | 用途 | 清理时机 |
|------|------|---------|
| `CapturedFrames/` 目录 | 存用户捕获的所有帧（每张原图可能 5-20MB） | **从来不清理！** |
| `SelectedMedia/` 临时目录 | 存用户选择的视频副本 | **只在 App 启动时清理** |

这就像**你租了个房间，往里搬东西但从来不扔**。用户可能今天用 App 捕获了 20 帧（≈200MB），明天又捕获了 30 帧（≈300MB），一个月后 App 占了好几个 GB。

更隐蔽的问题是：`MediaTempStore.removeAll()` 只在 `ZhenHaoXuanApp.init()` 里调用。如果用户**不杀 App**（iOS 会保持 App 在后台），临时文件就一直堆着。

#### 代码病灶

`FrameStore.swift` 整个类没有任何"我存了多少 MB"的统计，也没有"最多存 XX 张"的限制。`MediaTempStore` 只提供了 `removeAll()`，但除了启动时以外没人调用它。

#### 重构方案

**三层防护**（像手机相册的"最近删除"一样）：

1. **数量上限** — 最多保留 100 张帧
2. **容量上限** — 总共不超过 500MB
3. **自动清理** — 超出限制时，按时间从旧到新删除

```swift
// 改造后的 FrameStore
final class FrameStore {
    static let maxFrameCount = 100          // 最多 100 张
    static let maxTotalSize: Int64 = 500_000_000  // 最多 500MB
    
    func add(image: UIImage, ...) -> CapturedFrame? {
        // 写入前先检查容量
        enforceStorageLimits()
        
        // ... 现有写入逻辑 ...
    }
    
    private func enforceStorageLimits() {
        var records = readIndex()
        
        // 如果超过数量限制，删最旧的
        while records.count >= Self.maxFrameCount {
            if let oldest = records.first {
                removeFiles(for: oldest)
                records.removeFirst()
            }
        }
        
        // 如果超过容量，也删最旧的
        while totalSize() > Self.maxTotalSize, let oldest = records.first {
            removeFiles(for: oldest)
            records.removeFirst()
        }
    }
    
    private func totalSize() -> Int64 {
        // 遍历目录，累加文件大小
        // (具体实现使用 FileManager.contentsOfDirectory 的 fileSize 属性)
    }
}

// 改造 MediaTempStore：每次选完新视频时，删掉上一个临时视频
// 在 VideoPickerView 的 Coordinator 里：
func copyVideo(sourceURL: URL) -> URL {
    // 先删旧临时文件，再拷新的（而不是等 App 重启才清）
    MediaTempStore.removeAll()   // ← 新增这行
    let dest = MediaTempStore.makeDestinationURL(for: sourceURL)
    try? FileManager.default.copyItem(at: sourceURL, to: dest)
    return dest
}
```

> 💡 **对你的影响**：用户不会因为 App 占用太多空间而卸载（App Store 的"卸载未使用的 App"功能会优先推荐空间大户）。改动约 2-3 天。

---

### 🟡 问题 4：导出计数有"抢票漏洞"

**风险等级**: 🟡 中 | **优先级**: P2（下月处理）

#### 通俗解释

免费用户每天只能导出 3 次。但当前的计数方式有漏洞——如果用户在短时间内**快速点击导出**（例如选了 3 张图，连点 3 次），可能导致实际导出超过 3 次。

这就像**春运抢票软件**：12306 显示还剩 1 张票，两个人同时点"购买"，系统还没来得及更新库存，两人都买到了。

#### 代码病灶

`VIPManager.swift:410-426` — `recordExport()` 标记了 `@MainActor`，但调用它的 `FrameGalleryView` 里导出操作在后台线程读图、然后回调到主线程时才 +1：

```swift
// FrameGalleryView.swift:330-337
private func exportSelected(_ framesToExport: [CapturedFrame]) {
    viewModel.exportFrames(framesToExport) { success in
        if success {
            vipManager.recordExport(count: framesToExport.count)  // 回调时才计数
            // ⚠️ 如果用户在这个回调返回前又点了一次导出...
        }
    }
}
```

实际操作流程是：读图（后台线程耗时）→ 导出（系统相册操作耗时）→ 回调（+1）。在高延迟设备上，这段"窗口期"可能长达 2-3 秒。

#### 重构方案

**预扣模式**（像银行取钱：先冻结余额，取款成功才真正扣款）：

```swift
// 改造后的导出流程
func checkAndExportSelected() {
    let count = selectedFrames.count
    
    // 1. 先"预扣"：尝试冻结导出额度
    guard vipManager.reserveExportQuota(count: count) else {
        showExportLimitAlert = true
        return
    }
    
    // 2. 执行导出
    viewModel.exportFrames(framesToExport) { success in
        if success {
            vipManager.confirmExport(count: count)  // 确认扣款
        } else {
            vipManager.releaseExportQuota(count: count)  // 失败退款
        }
    }
}

// VIPManager 新增方法
private var reservedCount: Int = 0  // 已预扣但未确认的数量

func reserveExportQuota(count: Int) -> Bool {
    guard remainingExports - reservedCount >= count else { return false }
    reservedCount += count
    return true
}

func confirmExport(count: Int) {
    recordExport(count: count)
    reservedCount -= count
}

func releaseExportQuota(count: Int) {
    reservedCount -= count
}
```

> 💡 **对你的影响**：免费用户无法"卡 bug"突破每日 3 次的限制，保障付费转化。改动约 1 天。

---

### 🟢 问题 5：HDR 帧提取重复创建对象，浪费性能

**风险等级**: 🟢 低 | **优先级**: P3（有空优化）

#### 通俗解释

`FrameExtractorService.extractFrame()` 每次调用都创建一个新的 `AVAssetImageGenerator`。这就像**每次拍照都买一台新相机，拍完就扔**——不是不行，但浪费。

但对于 ≤30 秒的短视频（免费用户场景），这个浪费可以忽略不计。只有付费用户处理长视频时才有感知。

#### 重构方向

把 `AVAssetImageGenerator` 的创建改为**复用模式**：同一个视频 URL 共用一个 generator，视频切换时再重新创建。

> 💡 **对你的影响**：处理长视频时帧提取更快。现在不着急改。

---

### 📊 问题汇总表

| 编号 | 问题 | 风险 | 优先级 | 影响 | 开发天数 |
|------|------|------|--------|------|---------|
| 1 | 会员数据明文存储，可被篡改 | 🔴 高 | P0 | 收入损失 | 2-3 天 |
| 2 | 三种线程模型混用 | 🔴 高 | P1 | App 偶发崩溃 | 5-7 天 |
| 3 | 磁盘无上限，长期占满空间 | 🟡 中 | P1 | 用户卸载 | 2-3 天 |
| 4 | 导出计数有竞态窗口 | 🟡 中 | P2 | 少量收入损失 | 1 天 |
| 5 | 帧提取重复创建对象 | 🟢 低 | P3 | 长视频稍慢 | 1 天 |
| — | **无网络层（按你的决策：不需要服务器）** | — | — | 符合现状 | 0 |

---

## 第三部分：任务 B — Live Photo 提取功能设计

### 需求理解

> "在现有的视频选择器中，也支持选 Live Photo，然后提取 Live Photo 里那张静止的关键帧图片。"

Live Photo 本质上是一个 **3 秒的小视频 + 一张高分辨率静态照片** 的捆绑包。你要的"提取静态图片"就是从 Live Photo 里把那张高分辨率静态照片取出来。

### 交互方案

按你的选择（**方案 B**）：在选择器里同时支持视频和 Live Photo，用户一步选择。

```
现在的流程：
  打开选择器 → 只显示视频 → 选视频 → 进预览

改造后的流程：
  打开选择器 → 显示 视频 + Live Photo → 用户选一个
    ├── 选了视频 → 进预览（现有流程不变）
    └── 选了 Live Photo → 提取静态关键帧 → 进预览（显示单张静态图）
```

### 数据层融合设计

#### 新增数据模型

`CapturedFrame` 需要知道"这张图是从哪来的"：

```swift
// 新增来源类型
enum FrameSource {
    case video(url: URL)         // 从视频的某一帧提取
    case livePhoto(assetID: String)  // 从 Live Photo 提取
}

// 改造后的 CapturedFrame
struct CapturedFrame: Identifiable {
    let id: UUID
    let timestamp: TimeInterval    // Live Photo 来源时为 0
    let videoName: String          // Live Photo 来源时为 "Live Photo"
    let captureDate: Date
    let imageFileName: String
    let thumbnail: UIImage
    let source: FrameSource        // ← 新增字段
}
```

#### 新增服务：`LivePhotoExtractorService`

```swift
import Photos
import UIKit

class LivePhotoExtractorService {
    static let shared = LivePhotoExtractorService()
    private init() {}
    
    /// 从 Live Photo 的 PHAsset 中提取高分辨率静态关键帧
    func extractKeyImage(from asset: PHAsset) async throws -> UIImage {
        // Live Photo 包含一张高分辨率静态图（即"关键帧"）
        // 通过 PHImageManager 请求这张原始静态图即可
        
        return try await withCheckedThrowingContinuation { continuation in
            let options = PHImageRequestOptions()
            options.deliveryMode = .highQualityFormat
            options.isNetworkAccessAllowed = true  // iCloud 照片也能取
            options.isSynchronous = false
            
            PHImageManager.default().requestImage(
                for: asset,
                targetSize: PHImageManagerMaximumSize,  // 要原始尺寸
                contentMode: .aspectFit,
                options: options
            ) { image, info in
                if let error = info?[PHImageErrorKey] as? Error {
                    continuation.resume(throwing: error)
                } else if let image = image {
                    continuation.resume(returning: image)
                } else {
                    continuation.resume(throwing: LivePhotoError.extractionFailed)
                }
            }
        }
    }
}

enum LivePhotoError: LocalizedError {
    case extractionFailed
    var errorDescription: String? {
        "无法从 Live Photo 中提取图片，请重试。"
    }
}
```

#### 改造选择器入口

`VideoPickerView.swift` 需要改三处：

**1. 配置允许 Live Photo**：

```swift
// 改造前
configuration.filter = .videos

// 改造后
configuration.filter = .any(of: [.videos, .livePhotos])
configuration.selectionLimit = 1  // 保持不变
```

**2. 在 delegate 里判断类型并分发**：

```swift
func picker(_ picker: PHPickerViewController, didFinishPicking results: [PHPickerResult]) {
    picker.dismiss(animated: true) {
        guard let result = results.first else { return }
        
        // 判断用户选的是什么类型
        let isLivePhoto = result.itemProvider.hasItemConformingToTypeIdentifier(
            UTType.livePhoto.identifier
        )
        
        if isLivePhoto {
            self.handleLivePhotoSelection(result)
        } else {
            self.handleVideoSelection(result)  // 现有逻辑不变
        }
    }
}
```

**3. 新增 Live Photo 处理分支**：

```swift
private func handleLivePhotoSelection(_ result: PHPickerResult) {
    guard let assetID = result.assetIdentifier else {
        onError("无法获取 Live Photo 资源")
        return
    }
    
    DispatchQueue.main.async { self.isLoading = true }
    
    let fetchResult = PHAsset.fetchAssets(withLocalIdentifiers: [assetID], options: nil)
    guard let asset = fetchResult.firstObject else {
        DispatchQueue.main.async { self.isLoading = false }
        onError("无法获取 Live Photo 资源")
        return
    }
    
    Task {
        do {
            let image = try await LivePhotoExtractorService.shared.extractKeyImage(from: asset)
            
            await MainActor.run {
                self.isLoading = false
                // 通知上层：拿到了 Live Photo 的静态帧
                // 这里需要一个新的回调类型，下面会设计
                self.onLivePhotoExtracted(image, assetID)
            }
        } catch {
            await MainActor.run {
                self.isLoading = false
                self.onError(error.localizedDescription)
            }
        }
    }
}
```

#### 改造 HomeView 的回调接入

`HomeView.swift` 需要新增一个回调来处理"选了 Live Photo 得到图片"的情况：

```swift
// HomeView 新增状态
@State private var extractedLivePhotoImage: UIImage?
@State private var extractedLivePhotoAssetID: String?

// VideoPickerView 新增回调
VideoPickerView(
    onVideoSelected: { url in
        selectedVideoURL = url
    },
    onLivePhotoExtracted: { image, assetID in
        // Live Photo 提取完成后：
        // 1. 直接将图片存入 FrameStore
        // 2. 跳转到帧画廊展示
        extractedLivePhotoImage = image
        extractedLivePhotoAssetID = assetID
        handleLivePhotoCapture(image: image, assetID: assetID)
    },
    onVideoTooLong: { ... },
    onError: { ... },
    isLoading: $isLoading
)
```

#### Live Photo 提取后的处理

```swift
// HomeView 新增方法
private func handleLivePhotoCapture(image: UIImage, assetID: String) {
    // 复用现有 FrameStore，存盘逻辑不用变
    DispatchQueue.global(qos: .userInitiated).async {
        let frame = FrameStore.shared.add(
            image: image,
            timestamp: 0,
            videoName: "Live Photo"
        )
        DispatchQueue.main.async {
            self.captureViewModel.capturedFrames.append(frame!)
            // 直接打开画廊展示结果
            self.showGallery = true
        }
    }
}
```

### 完整数据流图

```
用户点「选择」
    │
    ▼
PHPicker（视频 + Live Photo）
    │
    ├── 选的是视频
    │     └── 现有流程：拷贝到临时目录 → 检查时长 → 进预览
    │
    └── 选的是 Live Photo
          └── 新流程：
                PHAsset（系统相册里的 Live Photo 记录）
                  │
                  ▼
                LivePhotoExtractorService
                  │  requestImage(asset, 原始尺寸)
                  ▼
                UIImage（高分辨率静态关键帧）
                  │
                  ▼
                FrameStore.add()（和视频帧用同一个仓库）
                  │
                  ▼
                CapturedFrame（source = .livePhoto）
                  │
                  ▼
                帧画廊展示 / 导出（复用现有导出逻辑）
```

> 💡 **对你的影响**：Live Photo 提取只需要新增约 100 行代码 + 改造约 30 行。核心存储和导出逻辑全部复用。开发约 2-3 天。

---

## 第四部分：综合优先级排期建议

### 推荐排期

```
第 1 周（P0 紧急）
├── 🔴 问题 1：会员数据迁移到 Keychain + 本地收据验证  [2-3 天]
└── 🟡 问题 3：存储上限 + 自动清理                      [2-3 天]

第 2 周（P1 重要）
├── 🟡 问题 4：导出计数预扣机制                          [1 天]
├── 🟢 问题 5：帧提取对象复用                            [1 天]
└── 🆕 任务 B：Live Photo 提取功能                       [2-3 天]

第 3-4 周（P1 架构升级）
└── 🔴 问题 2：统一 Swift Concurrency 模型               [5-7 天]
```

### 如果时间紧的"最小方案"

只做这三件事（约 6 天）就能解决最大的风险：

1. 会员数据搬 Keychain（2 天）
2. 存储上限 + 自动清理（2 天）  
3. Live Photo 提取（2 天）

线程模型统一可以往后排，毕竟现在 App 还能正常运行。

---

## 附录：通俗术语对照表

| 技术词 | 通俗解释 |
|--------|---------|
| UserDefaults | 系统给的免费小记事本，不加密 |
| Keychain | 苹果的加密保险柜，存在硬件安全芯片里 |
| GCD (DispatchQueue) | 喊一嗓子"你去干这个"，不管结果 |
| async/await | 写流程清单，一步步等结果 |
| @MainActor | 标签"这事必须在主线程做"（UI 只能在主线程刷新） |
| actor | 自动锁，同一时间只让一个人用 |
| 竞态条件 (Race Condition) | 两个人同时抢一张票，系统来不及反应 |
| NSLock | 手动上的锁，容易忘开 |
| AVAssetImageGenerator | 视频的"截图引擎" |
| PHAsset | 系统相册里的一张照片/视频的"户口本" |
| StoreKit 收据 | 苹果开的"付款凭证" |
| 序列化/反序列化 | 把内存里的数据打包存到硬盘（序列化），读回来拆包（反序列化） |
